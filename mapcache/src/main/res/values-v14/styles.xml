<resources>

    <!--
        Base application theme for API 14+. This theme completely replaces
        AppBaseTheme from BOTH res/values/styles.xml and
        res/values-v11/styles.xml on API 14+ devices.
    -->
    <style name="AppBaseTheme" parent="@style/Theme.MaterialComponents.Light">
        <!-- API 14 theme customizations can go here. -->
        <item name="colorPrimary">@color/nga_primary_primary</item>
        <item name="colorPrimaryVariant">@color/textPrimaryColor</item>
        <item name="colorOnPrimary">@color/black87</item>

        <item name="colorAccent">@color/nga_primary_light</item>
        <item name="colorSecondary">@color/nga_primary_light</item>
        <item name="colorSecondaryVariant">@android:color/holo_red_light</item>
        <item name="colorOnSecondary">@color/black87</item>
        <item name="android:itemBackground">@color/backgroundSecondaryColor</item>
    </style>

    <!-- Popup menus for edit button on map and map styles -->
    <style name="MyPopupMenu" parent="Widget.AppCompat.PopupMenu">
        <item name="android:textColor">@color/textPrimaryColor</item>
        <item name="android:itemBackground">@color/backgroundSecondaryColor</item>
        <item name="popupMenuBackground">@color/backgroundSecondaryColor</item>
    </style>

    <style name="MyActionBar" parent="@style/Widget.AppCompat.ActionBar.Solid">
        <item name="titleTextStyle">@style/MyTitleTextStyle</item>
        <item name="background">@color/nga_primary_primary_dark</item>
    </style>

    <style name="MyTitleTextStyle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/nga_accent_bright</item>
    </style>

    <style name="ButtonText">
        <item name="android:textColor">@color/black87</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
