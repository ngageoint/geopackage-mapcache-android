<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black50"
    android:orientation="vertical" >

    <ImageView
        android:id="@+id/basic_image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:paddingTop="16dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp">

        <!-- close preview -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/close_image_button"
            style="@style/MaterialButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginHorizontal="16dp"
            android:layout_weight="1"
            android:backgroundTint="@color/color_secondary_materialbutton"
            android:padding="8dp"
            android:textAlignment="center"
            android:textAppearance="@style/textAppearanceSubtitle1Heavy"
            android:textColor="@color/black87"
            android:text="@string/close" />

        <!-- delete button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/delete_image_button"
            style="@style/MaterialButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginHorizontal="16dp"
            android:layout_weight="1"
            android:backgroundTint="@color/color_states_material_delete"
            android:padding="8dp"
            android:textAlignment="center"
            android:textAppearance="@style/textAppearanceSubtitle1Heavy"
            android:textColor="@color/white"
            android:text="@string/delete" />

    </LinearLayout>

</LinearLayout>